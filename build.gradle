apply plugin: 'java'
apply plugin: 'maven'

group = 'org.cakelab.json'
version = '0.2.0'
archivesBaseName = 'json'

sourceSets.main {
    java.srcDirs = ['src']
    resources.srcDirs = ['resources']
}
